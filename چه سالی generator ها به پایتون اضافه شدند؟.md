Generatorها در **Python 2.2** معرفی شدند؛ یعنی حدود **سال 2001**. این قابلیت از طریق **PEP 255 — Simple Generators** وارد Python شد و هدف اصلی‌اش این بود که بتوانیم iteratorهایی بسازیم که وضعیت اجرای خودشان را حفظ کنند، بدون اینکه مجبور باشیم کل داده‌ها را در حافظه نگه داریم.

بعداً در **Python 2.5 (سال 2006)** قابلیت مهم `send()` و `yield expression` اضافه شد (**PEP 342**) و Generatorها از یک ابزار صرفاً برای تولید داده، به سازوکاری قدرتمندتر برای coroutineها تبدیل شدند. سپس در **Python 3.3 (سال 2012)**، [[Yield from]] (**PEP 380**) اضافه شد که کار با Generatorهای تو‌در‌تو را بسیار ساده‌تر کرد.

[[Generators در پایتون]]