نکته مهم این است که خود Agent معمولاً یک موجودیت جدا از LLM نیست؛ **Agent یک سیستم/منطق اجرایی است که از LLM برای تصمیم‌گیری استفاده می‌کند.**

ترتیب ساده‌تر:

1. **User** → سؤال را می‌پرسد.
    
2. **Agent/Harness** → درخواست را بررسی و برنامه‌ریزی می‌کند.
    
3. **RAG** → دانش مرتبط را بازیابی می‌کند.
    
4. **Skill** → دستورالعمل و روش انجام کار را در اختیار Agent قرار می‌دهد.
    
5. **Context** → سؤال + اطلاعات RAG + Skill + وضعیت فعلی جمع می‌شوند.
    
6. **LLM** → بر اساس Context تصمیم می‌گیرد.
    
7. اگر لازم باشد → **Tool** اجرا می‌شود.
    
8. نتیجه Tool دوباره وارد **Context** می‌شود.
    
9. **LLM** بر اساس Context جدید پاسخ نهایی را تولید می‌کند.
    
10. **User** → پاسخ را دریافت می‌کند.

[[AI]]
[[بصورت لیست نشون بده]]