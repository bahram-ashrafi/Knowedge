تفاوت اصلی این است:

- **Agent** = موجودی که **تصمیم می‌گیرد چه کاری انجام دهد**. معمولاً از LLM استفاده می‌کند، هدف دارد و می‌تواند بین چند مرحله تصمیم‌گیری کند.
    
- **Harness** = سیستمی که **Agent را اجرا و پشتیبانی می‌کند**؛ ابزارها، context، حافظه، دسترسی به فایل/سیستم، اجرای toolها، محدودیت‌ها و چرخه اجرای Agent را مدیریت می‌کند.
    

مثلاً در یک **Frontend Coding Agent**:  
`User → Harness → Agent/LLM → Tool (Figma/Git/Terminal) → نتیجه`  
اینجا Agent می‌گوید «اول Figma را بررسی کنم، بعد کد بنویسم»، ولی Harness امکان دسترسی به Figma، اجرای command، مدیریت context و برگرداندن نتیجه را فراهم می‌کند.

[[AI، هارنس (AI Harness)]]