نه، **همه‌ی Context را تو آماده نمی‌کنی**. در یک Agent معمولاً خود **Agent Framework** بخش زیادی از Context را جمع و آماده می‌کند؛ مثلاً پیام فعلی کاربر، تاریخچه مکالمه، نتایج Toolها و تعریف ابزارها را کنار هم قرار می‌دهد و برای LLM می‌فرستد.

اما تو به‌عنوان سازنده Agent باید مشخص کنی **چه اطلاعاتی باید وارد Context شوند و چه ابزارهایی در اختیار LLM باشند**. مثلاً:

```text
تو → تعریف Agent + ابزارها + قوانین
کاربر → پیام
Agent → جمع‌کردن Context
Agent → ارسال Context + Tools → LLM
LLM → تصمیم / Tool Call
Agent → اجرای Tool
Agent → نتیجه را به LLM
LLM → پاسخ نهایی
```

پس معمولاً **تو Context را دستی برای هر سؤال نمی‌سازی؛ معماری Agent این کار را انجام می‌دهد.**

[[وقتی میخاد order رو بگیره چطور ارتباط میگیره با agent?]]