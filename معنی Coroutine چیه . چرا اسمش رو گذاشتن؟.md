**Coroutine** از ترکیب دو واژه ساخته شده:

> **Co + Routine**

`Routine` یعنی یک **روال/تابع اجرایی** و `Co` از مفهوم **Cooperative** می‌آید؛ یعنی «همکارانه». بنابراین Coroutine را می‌توان تقریباً **«روال اجرایی همکارانه»** ترجمه کرد.

چرا «همکارانه»؟ چون چند Coroutine می‌توانند در یک برنامه با هم کار کنند و هرکدام **داوطلبانه اجرای خودشان را متوقف کنند و فرصت اجرا را به دیگری بدهند**:

```text
Coroutine A
    │
    ├── اجرا
    ├── await → «فعلاً من متوقف می‌شوم»
    │
    ▼
Coroutine B
    │
    ├── اجرا
    ├── await
    │
    ▼
Coroutine A
    └── ادامه از همان نقطه
```

در مقابل **[[Thread]]** معمولاً توسط سیستم‌عامل زمان‌بندی می‌شود؛ اما Coroutine در مدل asynchronous معمولاً با همکاری خود برنامه و **Event Loop** مدیریت می‌شود. پس اسم `Coroutine` در اصل به این ایده اشاره دارد: **چند روال اجرایی که می‌توانند اجرای خود را متوقف کنند و به یکدیگر فرصت اجرا بدهند.**

[[توضیح بده coroutine]]