خود **Generator را نمی‌توان reset کرد**. وقتی به پایان رسید، state آن تمام شده و نمی‌توانی همان Generator را به ابتدای تابع برگردانی.

اما خیلی ساده می‌توانی **یک Generator جدید** بسازی:

```python
def count_up():
    for i in range(3):
        yield i

gen = count_up()

print(list(gen))  # [0, 1, 2]
print(list(gen))  # []

gen = count_up()  # Generator جدید
print(list(gen))  # [0, 1, 2]
```

پس تفاوت این دو مهم است:

```python
gen = count_up()   # ساخت Generator جدید
```

در مقابل:

```python
gen = ...          # reset کردن Generator موجود ❌
```

اگر به reset شدن مکرر نیاز داری، معمولاً به‌جای نگه‌داشتن Generator، **خود تابع Generator را نگه می‌داری و هر بار آن را دوباره صدا می‌زنی**.

[[Generators در پایتون]]