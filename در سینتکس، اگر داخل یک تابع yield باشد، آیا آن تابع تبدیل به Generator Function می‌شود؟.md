اگر منظورت این است که **«در سینتکس، اگر داخل یک تابع `yield` باشد، آیا آن تابع تبدیل به Generator Function می‌شود؟»** بله، دقیقاً. وجود `yield` در بدنه‌ی تابع باعث می‌شود پایتون آن تابع را یک **Generator Function** در نظر بگیرد:

```python
def test():
    yield 10
```

حالا:

```python
result = test()
```

`test()` مثل یک تابع معمولی اجرا نمی‌شود که `10` را برگرداند؛ بلکه یک **Generator Object** ایجاد می‌کند. اجرای واقعی تابع زمانی شروع می‌شود که `next(result)` یا مثلاً `for` روی آن انجام دهی. بنابراین می‌توانی این قانون ذهنی را داشته باشی: **`return` → تابع معمولی، `yield` → Generator Function**. البته `return` می‌تواند داخل Generator هم وجود داشته باشد؛ در آن حالت Generator را تمام می‌کند.

[[Generators در پایتون]]