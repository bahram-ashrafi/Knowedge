**Worker (ورکر)** در برنامه‌نویسی یعنی یک **اجراکنندهٔ مستقل برای انجام کار**؛ معمولاً کاری که نمی‌خواهیم مستقیماً در مسیر اصلی برنامه انجام شود.

مثلاً در یک وب‌سرور:

```text
Client
  │
  ▼
Web Server
  │
  ├── Worker 1 → درخواست A
  ├── Worker 2 → درخواست B
  └── Worker 3 → درخواست C
```

هر Worker می‌تواند بخشی از کار را انجام دهد. بسته به تکنولوژی، Worker ممکن است یک **Process، Thread یا یک اجرای پس‌زمینه** باشد. مثلاً در **FastAPI/Uvicorn** وقتی می‌گویی `--workers 4`، معمولاً یعنی چهار **process** از برنامه اجرا می‌شوند تا بتوانند درخواست‌ها را بین خودشان تقسیم کنند.

یک نکتهٔ مهم: **Worker با Task یکی نیست.** در بحث Async که اخیراً داشتیم، یک Process/Worker می‌تواند تعداد زیادی `Task` را توسط یک Event Loop اجرا کند:

```text
Worker / Process
      │
      ▼
 Event Loop
   ├── Task A
   ├── Task B
   ├── Task C
   └── Task D
```

پس اگر بخواهیم خیلی بنیادی نگاه کنیم: **Worker = چه چیزی کار را اجرا می‌کند**، در حالی که **Task = چه کاری باید اجرا شود**.

[[Python]]