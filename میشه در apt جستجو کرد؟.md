بله. با `apt` می‌توانی در **لیست پکیج‌های موجود در Repositoryها** جستجو کنی:

```bash
apt search nginx
```

مثلاً:

```bash
apt search python3
```

اگر فقط بخواهی اطلاعات یک پکیج خاص را ببینی:

```bash
apt show nginx
```

تفاوت مهم این است که `apt search` در **نام و توضیحات پکیج‌ها** جستجو می‌کند؛ یعنی مثلاً ممکن است با `apt search web server` چندین پکیج مرتبط پیدا شود.

[[apt چیست؟]]