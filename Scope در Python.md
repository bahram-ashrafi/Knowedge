این صفحه درباره‌ی **Scope در Python** است؛ یعنی «یک نام/متغیر را Python در چه محدوده‌ای می‌تواند پیدا کند و به آن دسترسی داشته باشد». W3Schools چهار محدوده را در قالب قانون **LEGB** معرفی می‌کند: **Local → Enclosing → Global → Built-in**. ([w3schools.com](https://www.w3schools.com/python/python_scope.asp "Python Scope")) urlصفحه W3Schools درباره Python Scope[https://www.w3schools.com/python/python_scope.asp](https://www.w3schools.com/python/python_scope.asp%EE%88%81%EE%88%81)

برای درک عمیق‌تر، این‌طور تصور کن: هر `function` یک **محیط نام‌گذاری (namespace)** دارد. اگر داخل تابع بنویسی `x = 10`، این `x` به‌صورت پیش‌فرض local است. اگر تابع دیگری داخل آن تابع داشته باشیم، تابع داخلی می‌تواند `x` تابع بیرونی را ببیند؛ این همان **Enclosing** است. بیرون از همه‌ی توابع، متغیرهایی مثل `x = 100` در **Global** هستند و در نهایت اگر Python نامی را در هیچ‌کدام پیدا نکند، سراغ **Built-in** می‌رود؛ مثلاً `print` و `len`. ([w3schools.com](https://www.w3schools.com/python/python_scope.asp "Python Scope"))

نکته‌ی خیلی مهم این صفحه برای ادامه‌ی یادگیری Python این سه کلمه است: **`global`** یعنی «من می‌خواهم به متغیر Global اشاره کنم و حتی آن را تغییر بدهم»، و **`nonlocal`** یعنی «من می‌خواهم به متغیر موجود در تابع بیرونی دسترسی پیدا کنم و آن را تغییر بدهم». بنابراین Scope فقط بحث «دسترسی به متغیر» نیست؛ پایه‌ی فهم **nested functions، closure، decorator و حتی بخشی از رفتار generatorها** هم هست. W3Schools همین موضوع را بعد از Scope با `global`، `nonlocal` و LEGB توضیح می‌دهد. ([w3schools.com](https://www.w3schools.com/python/python_scope.asp "Python Scope"))
[[Python]]