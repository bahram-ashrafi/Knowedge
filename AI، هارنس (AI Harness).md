در **AI**، هارنس (AI Harness) یعنی **لایه‌ای که یک مدل AI/LLM را در یک محیط اجرایی قرار می‌دهد و امکانات لازم برای کار کردن آن را فراهم می‌کند**.

مثلاً یک Agent را در نظر بگیر: خود LLM فقط تصمیم می‌گیرد «چه کاری انجام شود»، اما Harness چیزهایی مثل **ابزارها (Tools)، Context، حافظه، اجرای کد، دسترسی به فایل‌ها، مدیریت مراحل اجرای Agent و محدودیت‌ها** را فراهم می‌کند. بنابراین می‌توان گفت: **LLM = مغز، Agent = موجودی که هدف دارد، Harness = محیط و سیستم اجرایی‌ای که Agent را به ابزار و منابع متصل می‌کند.**

[[AI]]