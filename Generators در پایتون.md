در پایتون، **Generator** روشی برای تولید داده‌ها به‌صورت **یکی‌یکی و در زمان نیاز (Lazy)** است، نه اینکه همه‌ی داده‌ها را از ابتدا در حافظه بسازد. تفاوت اصلی آن با `list` این است که لیست مثلاً `range(1_000_000)` را اگر به شکل لیست بسازی، میلیون‌ها مقدار را در حافظه نگه می‌دارد؛ اما Generator فقط **مقدار فعلی و وضعیت اجرای خودش** را نگه می‌دارد و هر بار که مقدار بعدی را بخواهی، ادامه‌ی اجرا را انجام می‌دهد.

مهم‌ترین ابزار برای ساخت Generator، `yield` است:

```python
def numbers():
    yield 1
    yield 2
    yield 3

g = numbers()

print(next(g))  # 1
print(next(g))  # 2
print(next(g))  # 3
print(next(g))  # StopIteration
```

اینجا نکته‌ی بسیار مهم این است که با فراخوانی `numbers()`، بدنه‌ی تابع **اجرا نمی‌شود**؛ یک Generator Object ساخته می‌شود. `next()` اجرای تابع را تا رسیدن به `yield` ادامه می‌دهد، مقدار را برمی‌گرداند و اجرای تابع را همان‌جا **متوقف می‌کند**. دفعه‌ی بعدی `next()`، تابع دقیقاً از همان نقطه ادامه پیدا می‌کند. بنابراین `yield` برخلاف `return` باعث پایان کامل تابع نمی‌شود؛ بلکه **state اجرای تابع را حفظ می‌کند**.

مثلاً:

```python
def count():
    n = 0
    while True:
        yield n
        n += 1

g = count()

for i in range(5):
    print(next(g))
```

خروجی:

```text
0
1
2
3
4
```

این Generator حتی **بی‌نهایت** است، ولی مشکلی ایجاد نمی‌کند چون همه‌ی اعداد را یکجا تولید نمی‌کند. این دقیقاً جایی است که Generator در Python بسیار قدرتمند می‌شود: **پردازش فایل‌های بزرگ، stream داده، pipelineهای پردازشی، و داده‌هایی که نمی‌خواهیم یکجا در RAM قرار بگیرند.** در ادامه‌ی بحث Python Data Model، قدم مهم بعدی این است که بفهمیم `generator` دقیقاً چه رابطه‌ای با **[[Generator، Iterator و Iterable چه رابطه‌ای دارند؟]]** 

[[Python]]