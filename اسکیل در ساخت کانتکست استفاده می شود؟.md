بله، **Skill می‌تواند در ساخت Context برای Agent نقش داشته باشد**، اما Skill و Context یکی نیستند. Skill معمولاً دستورالعمل‌های تخصصی و قواعد انجام کار را در اختیار Agent می‌گذارد؛ Agent هنگام انجام وظیفه، بر اساس آن Skill و اطلاعات مرتبط، **Context مناسب را تشکیل می‌دهد**.

مثلاً در Frontend Agent: کاربر می‌گوید «این صفحه Figma را پیاده‌سازی کن» → Agent از **Figma Skill** استفاده می‌کند → Skill می‌گوید چه اطلاعاتی از Figma و چه قواعدی باید بررسی شوند → Agent اطلاعات لازم مثل Design، کامپوننت‌ها، توکن‌ها و ساختار پروژه را وارد **Context** می‌کند → سپس LLM بر اساس آن Context کد تولید می‌کند.

[[skill چیه؟]]