**`snapd` یک سرویس (daemon) در پس‌زمینه‌ی Ubuntu است که کل سیستم Snap را مدیریت می‌کند.** یعنی خود دستور `snap` بیشتر نقش **CLI** را دارد، اما `snapd` کارهای اصلی را انجام می‌دهد.

مثلاً وقتی می‌زنی:

```bash
snap install code
```

تقریباً این اتفاق می‌افتد:

```text
شما
 ↓
snap  ← دستور خط فرمان
 ↓
snapd ← سرویس مدیریت Snap
 ↓
Snap Store
 ↓
دانلود و نصب Snap
 ↓
اجرای برنامه + sandbox + مدیریت permission
```

`systemd` معمولاً `snapd` را به‌عنوان یک سرویس اجرا می‌کند. می‌توانی وضعیتش را ببینی:

```bash
systemctl status snapd
```

پس خیلی ساده: **`snap` = ابزار خط فرمان**، **`snapd` = موتور/سرویس پشت Snap**.

[[snap هم یک پیکیج منجر است؟]]