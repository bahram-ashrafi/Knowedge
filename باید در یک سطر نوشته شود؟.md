بله، بدنه‌ی lambda فقط یک Expression است و معمولاً در یک سطر نوشته می‌شود.

Python

Run

```
square = lambda x: x * x
```

اما می‌تواند از نظر ظاهری در چند خط هم شکسته شود، اگر داخل پرانتز باشد؛ چیزی که مهم است این است که فقط یک expression داشته باشد، نه چند دستور (`statement`).

Python

Run

```
result = lambda x: (
    x * x
)
```

مثلاً این مجاز نیست چون `print` و `return` و انتساب جزو statement هستند:

Python

Run

```
# خطا
lambda x:
    y = x * 2
    return y
```

پس قانون اصلی این نیست که حتماً یک خط باشد؛ قانون اصلی این است که lambda فقط یک expression دارد.
[[Lambda Function]]