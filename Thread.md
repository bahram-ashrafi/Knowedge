**Thread (نخ)** را می‌توان یک **مسیر مستقل اجرای دستورها** داخل یک Process دانست. یک Process می‌تواند چند Thread داشته باشد و این Threadها معمولاً حافظه‌ی Process را با هم **share** می‌کنند:

```text
Process
│
├── Thread 1 → اجرای کد
├── Thread 2 → اجرای کد
└── Thread 3 → اجرای کد
```

مثلاً اگر برنامه‌ای سه کار داشته باشد، می‌تواند آن‌ها را روی سه Thread اجرا کند. روی یک CPU تک‌هسته‌ای، CPU بین Threadها سریع جابه‌جا می‌شود (**context switching**) و ظاهراً همزمان اجرا می‌شوند؛ روی CPU چند‌هسته‌ای، Threadهای مختلف واقعاً می‌توانند **به‌صورت همزمان** روی هسته‌های مختلف اجرا شوند.

تفاوت مهم با Coroutine این است که **Thread توسط سیستم‌عامل زمان‌بندی می‌شود**؛ یعنی OS تصمیم می‌گیرد چه زمانی Thread متوقف و Thread دیگری اجرا شود. اما Coroutine معمولاً در سطح برنامه و توسط **Event Loop/runtime** مدیریت می‌شود و خودش در نقاطی مثل `await` فرصت اجرا را واگذار می‌کند. بنابراین یک تصویر ذهنی خوب این است: **Process = محیط اجرا، Thread = مسیر اجرای واقعی، Coroutine = کار قابل توقف/ادامه درون آن مسیر.**