۱. **Agent قبل از فرستادن درخواست به LLM، لیست ابزارهای موجود را به LLM معرفی می‌کند.** مثلاً به LLM می‌گوید: «تو به این ابزار دسترسی داری: `get_order_status(order_id)`؛ این ابزار وضعیت یک سفارش را برمی‌گرداند.» این اطلاعات معمولاً در قالب **Tool Definition / Function Schema** داخل درخواست به مدل قرار می‌گیرد.

۲. بنابراین LLM از قبل `get_order_status` را در «دانش خود» نداشته؛ **Agent آن را در context همان درخواست به مدل معرفی کرده است.** بعد LLM وقتی سؤال «وضعیت سفارش ۱۲۳ را بررسی کن» را می‌بیند، از بین ابزارهای معرفی‌شده تشخیص می‌دهد که باید `get_order_status` را با `order_id=123` صدا بزند.

[[agent vs harness]]
[[AI، هارنس (AI Harness)]]
