**Sandbox** یعنی برنامه را تا حدی در یک **محیط محدودشده** اجرا کنیم تا نتواند آزادانه به همه قسمت‌های سیستم دسترسی داشته باشد. مثلاً یک Snap ممکن است اجازه داشته باشد به فایل‌های Home دسترسی داشته باشد، اما اجازه دسترسی مستقیم به بعضی قسمت‌های سیستم را نداشته باشد. این دسترسی‌ها با مفهوم **interface / permission** کنترل می‌شوند. مثلاً:

```bash
snap connections code
```

می‌توانی ببینی برنامه به چه منابعی دسترسی دارد. بنابراین اگر یک برنامه مشکل امنیتی داشته باشد، sandbox می‌تواند محدوده آسیب را کاهش دهد.

**Rollback** یعنی اگر نسخه جدید یک Snap مشکل داشت، بتوانی به نسخه قبلی برگردی. Snap معمولاً نسخه قبلی را نگه می‌دارد؛ مثلاً فرض کن `app` از نسخه `2.0` به `2.1` آپدیت شده و `2.1` خراب است. می‌توانی بگویی:

```bash
snap revert app
```

و Snap آن را به revision قبلی برمی‌گرداند. بنابراین یکی از تفاوت‌های مهم Snap این است که **آپدیت و برگشت به نسخه قبلی را به‌صورت یکپارچه مدیریت می‌کند**.

[[snap هم یک پیکیج منجر است؟]]