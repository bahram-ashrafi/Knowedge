۱. **LLM مستقیماً با Agent ارتباط شبکه‌ای یا مستقیم ندارد.** LLM فقط یک خروجی ساختاریافته تولید می‌کند که می‌گوید: «ابزار `get_order_status` را با `order_id=123` اجرا کن.»

۲. **Agent این خروجی را دریافت می‌کند و خودش Tool را اجرا می‌کند**؛ مثلاً تابع Python یا API را صدا می‌زند. سپس نتیجه را دوباره به LLM می‌دهد:

```text
LLM → Tool Call: get_order_status(123)
       ↓
Agent → اجرای تابع/API
       ↓
Database → وضعیت سفارش ۱۲۳
       ↓
Agent → نتیجه به LLM
       ↓
LLM → پاسخ نهایی
```

یعنی **Agent واسطه بین LLM و دنیای واقعی است**.

[[llm چطور میفهمد که ایجنت ابزار  get_order_status را دارد؟]]