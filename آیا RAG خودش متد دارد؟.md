بله. **RAG خودش یک روش/معماری است، نه یک متد واحد**؛ ولی برای پیاده‌سازی آن مجموعه‌ای از عملیات یا متدها وجود دارد.

مثلاً:

1. `chunk()` → تقسیم اسناد به بخش‌های کوچک
    
2. `embed()` → تبدیل بخش‌ها به embedding
    
3. `retrieve()` → پیدا کردن بخش‌های مرتبط
    
4. `rerank()` → مرتب‌سازی نتایج بر اساس ارتباط
    
5. `build_context()` → ساخت Context برای LLM
    
6. `generate()` → ارسال Context به LLM و تولید پاسخ
    

بنابراین وقتی می‌گوییم «RAG داریم»، منظور یک **pipeline از چند عملیات** است، نه اینکه RAG خودش یک API یا یک متد خاص باشد.

[[AI RAG]]