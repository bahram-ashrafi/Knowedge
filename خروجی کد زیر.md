```
def count_up():
    for i in range(3):
        yield i

gen = count_up()
print(list(gen))
```

بله، این کد کاملاً صحیح است و خروجی آن:

```text
[0, 1, 2]
```

اتفاقی که می‌افتد این است:

```python
gen = count_up()
```

اینجا هنوز `for` اجرا نشده؛ فقط یک **Generator Object** ساخته شده است. سپس:

```python
list(gen)
```

Generator را **کامل مصرف** می‌کند. یعنی تقریباً `next(gen)` را پشت‌سرهم اجرا می‌کند:

```text
next(gen) → 0
next(gen) → 1
next(gen) → 2
next(gen) → StopIteration
```

و این مقادیر را داخل یک `list` قرار می‌دهد. بنابراین بعد از اجرای `list(gen)`، Generator دیگر چیزی برای تولید ندارد:

```python
print(list(gen))  # [0, 1, 2]
print(list(gen))  # []
```

این نکته برای درک Generator خیلی مهم است: **Generator یک جریان یک‌بارمصرف از داده‌هاست، نه یک container مثل `list`.**

[[Generators در پایتون]]