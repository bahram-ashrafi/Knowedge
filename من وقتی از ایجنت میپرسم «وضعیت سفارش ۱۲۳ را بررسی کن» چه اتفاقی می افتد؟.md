۱. **سؤال کاربر:** «وضعیت سفارش ۱۲۳ را بررسی کن»  
۲. **Agent:** درخواست را دریافت می‌کند و آن را به سیستم LLM می‌دهد.  
۳. **LLM:** تشخیص می‌دهد برای پاسخ باید اطلاعات سفارش ۱۲۳ را از سیستم سفارش‌ها بگیرد.  
۴. **LLM → Tool:** درخواست استفاده از ابزار `get_order_status(123)` را صادر می‌کند.  
۵. **Agent:** این Tool را اجرا می‌کند و نتیجه را از API یا Database می‌گیرد.  
۶. **Tool → Agent:** مثلاً برمی‌گرداند: «سفارش ۱۲۳ ارسال شده است.»  
۷. **Agent → LLM:** نتیجه را دوباره در اختیار LLM قرار می‌دهد.  
۸. **LLM:** نتیجه را تفسیر می‌کند و پاسخ مناسب می‌سازد.  
۹. **Agent → کاربر:** «سفارش ۱۲۳ ارسال شده است.»
[[agent vs harness]]