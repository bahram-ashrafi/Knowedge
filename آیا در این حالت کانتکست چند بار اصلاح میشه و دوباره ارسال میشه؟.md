بله، **ممکن است چندین بار Context تغییر کند و دوباره به LLM ارسال شود**؛ این یکی از ویژگی‌های مهم Agent است. مثلاً:

`Context اولیه → LLM → تصمیم به اجرای Tool → Tool → نتیجه Tool به Context اضافه می‌شود → LLM دوباره → تصمیم بعدی → Tool → Context جدید → LLM ...`

بنابراین Context معمولاً یک چیز کاملاً ثابت نیست. در هر **Iteration/Loop** ممکن است اطلاعاتی به آن اضافه یا بعضی اطلاعات کم/خلاصه شوند و سپس نسخه جدید Context به LLM داده شود. مثلاً Agent ابتدا از RAG مستندات Odoo را پیدا می‌کند، بعد LLM تصمیم می‌گیرد یک Tool برای خواندن فایل اجرا شود؛ نتیجه فایل وارد Context می‌شود و LLM دوباره آن را می‌بیند و تصمیم بعدی را می‌گیرد.

[[مراحل به چه صورت است]]