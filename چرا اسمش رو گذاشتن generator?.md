چون **Generator واقعاً «تولیدکننده» مقدار است**؛ برخلاف یک `list` که مقادیر را از قبل در خودش نگه می‌دارد، Generator هر بار که از آن مقدار بخواهی، **یک مقدار جدید تولید می‌کند**:

```python
def numbers():
    for i in range(3):
        yield i

gen = numbers()

next(gen)  # تولید → 0
next(gen)  # تولید → 1
next(gen)  # تولید → 2
```

اسمش از همین مفهوم می‌آید: **Generator = چیزی که داده تولید می‌کند**. نکته‌ی جالب این است که Generator معمولاً همه‌ی داده‌ها را یکجا تولید نمی‌کند؛ بلکه می‌توانی آن را مثل یک **ماشین تولید مقدار** تصور کنی: `next()` را می‌زنی → یک مقدار تولید می‌کند → متوقف می‌شود → دوباره `next()` → مقدار بعدی را تولید می‌کند. به همین دلیل مفهوم اصلی آن **lazy generation** است.

[[Generators در پایتون]]