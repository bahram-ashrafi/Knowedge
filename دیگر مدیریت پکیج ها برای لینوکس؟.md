بسته به توزیع لینوکس، Package Manager متفاوت است:

- **Debian / Ubuntu** → `apt` و سطح پایین‌تر `dpkg`
    
- **Fedora / RHEL / CentOS** → `dnf` و سطح پایین‌تر `rpm`
    
- **Arch Linux** → `pacman`
    
- **openSUSE** → `zypper`
    
- **Alpine Linux** → `apk`
    

مثلاً در Ubuntu می‌نویسی `apt install nginx`، در Fedora معمولاً `dnf install nginx` و در Arch می‌نویسی `pacman -S nginx`. نکته مهم: `apt` خودش به `dpkg` متکی است؛ یعنی `apt` مدیریت سطح بالاتر را انجام می‌دهد، مثل پیدا کردن وابستگی‌ها و دریافت پکیج از Repository، و `dpkg` عملیات نصب واقعی فایل `.deb` را انجام می‌دهد.

[[apt چیست؟]]