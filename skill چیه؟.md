در زمینه‌ی **AI Agent**، یک **Skill** یعنی یک مجموعه دستورالعمل و دانش تخصصی که به Agent می‌گوید **برای انجام یک نوع کار مشخص، چگونه عمل کند**. مثلاً Skill به نام `figma-design-to-code` می‌تواند به Agent بگوید وقتی قرار است یک طراحی Figma را به کد تبدیل کند، چه مراحلی را انجام دهد، از چه ابزارهایی استفاده کند و چه قواعدی را رعایت کند.

پس می‌توانی Skill را شبیه یک **راهنمای تخصصی قابل‌استفاده توسط Agent** در نظر بگیری. مثلاً: `Git Skill` برای کار با Git، `Figma Skill` برای کار با Figma، یا `Odoo Skill` برای توسعه Odoo. خود Skill معمولاً **مدل AI نیست**؛ بلکه دستورالعملی است که توانایی مدل را برای یک کار خاص سازمان‌دهی و هدایت می‌کند.

[[AI]]