`apt` مخفف **Advanced Package Tool** است و در لینوکس‌های مبتنی بر Debian مثل Ubuntu، ابزار مدیریت **پکیج‌ها (نرم‌افزارها)** است.

وقتی می‌نویسی:

```bash
apt install nginx
```

در واقع به سیستم می‌گویی: **«پکیج nginx را از مخازن نرم‌افزاری پیدا کن، دانلود کن و نصبش کن.»**  
`apt` خودش نرم‌افزار نیست؛ یک **Package Manager** است که کارهایی مثل نصب، حذف، آپدیت و مدیریت وابستگی‌های نرم‌افزارها را انجام می‌دهد. مثلاً `apt update` فهرست پکیج‌های موجود را به‌روز می‌کند و `apt upgrade` پکیج‌های نصب‌شده را ارتقا می‌دهد.