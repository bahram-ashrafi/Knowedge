بله، **Generatorها هنوز کاملاً پرکاربردند** و منسوخ نشده‌اند. در واقع در Python مدرن هنوز یکی از ابزارهای اصلی برای **lazy evaluation، پردازش داده‌های بزرگ، streamها و کاهش مصرف حافظه** هستند. مثلاً اگر یک فایل چند گیگابایتی داشته باشی، معمولاً نمی‌خواهی کل آن را با `read()` وارد RAM کنی:

```python
def read_lines(file):
    with open(file) as f:
        for line in f:
            yield line
```

اینجا خط‌ها یکی‌یکی تولید می‌شوند. یا در pipelineها:

```python
lines = read_lines("huge.log")
errors = (line for line in lines if "ERROR" in line)

for error in errors:
    print(error)
```

**جایگزین نشده‌اند؛ بلکه کاربردشان تخصصی‌تر و مشخص‌تر شده است.** برای asynchronous programming، `async/await` جای الگوهای قدیمی coroutine مبتنی بر Generator را گرفته؛ اما برای **تولید lazy داده، iteration، pipeline پردازش داده و کار با داده‌های بزرگ** همچنان Generator انتخاب بسیار خوبی است. بنابراین این دو را جایگزین یکدیگر ندان: `yield` بیشتر می‌گوید **«داده را هنگام نیاز تولید کن»**، در حالی که `await` می‌گوید **«منتظر یک عملیات asynchronous بمان و در این فاصله event loop را آزاد کن»**.

[[Generators در پایتون]]