بله، **تقریباً می‌توانی این‌طور بگویی**، ولی عبارت دقیق‌تر این است: **Snapها به‌صورت پیش‌فرض با مکانیزم‌های sandboxing اجرا می‌شوند**. یعنی `snapd` و سیستم‌های امنیتی لینوکس مثل **AppArmor** و **seccomp** دسترسی Snap را محدود می‌کنند.

اما sandbox به معنی «یک ماشین مجازی یا محیط کاملاً جدا» نیست. Snap همچنان از **Kernel و منابع سیستم اصلی** استفاده می‌کند و بسته به permissionهایی که دارد می‌تواند به بخش‌هایی از سیستم دسترسی داشته باشد. بنابراین بهتر است بگویی: **«Snapها به‌صورت پیش‌فرض sandboxed هستند و دسترسی‌شان توسط interfaceها و policyهای امنیتی کنترل می‌شود.»**

[[rollback، sandbox اینا رو بیشتر توضیح بده]]