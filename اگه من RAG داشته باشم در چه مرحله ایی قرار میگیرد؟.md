اگر **RAG** داشته باشی، معمولاً در بخش **Context/Knowledge** معماری Agent قرار می‌گیرد؛ یعنی قبل از اینکه LLM تصمیم نهایی را بگیرد، RAG اطلاعات مرتبط را از منابع تو پیدا می‌کند و به Context مدل اضافه می‌کند.

مثلاً در Agent مربوط به Odoo:

`User → Agent → RAG (جستجوی مستندات/UI Kit/کدها) → Context → LLM → Tool → نتیجه`

پس **RAG خودش Agent نیست** و معمولاً Tool هم نیست؛ یک مکانیزم برای **بازیابی دانش مرتبط و تزریق آن به Context** است.
[[AI RAG]]