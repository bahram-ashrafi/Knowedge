نه. در **Ubuntu** چند ابزار برای مدیریت نرم‌افزار وجود دارد، ولی ابزار اصلی و استاندارد **`apt`** است:

- **`apt`** → ابزار اصلی و پیشنهادی برای نصب، حذف، جستجو و آپدیت پکیج‌ها.
    
- **`dpkg`** → سطح پایین‌تر؛ مستقیماً با فایل‌های `.deb` کار می‌کند.
    
- **`snap`** → مدیریت Snapها؛ مثلاً `snap install code`.
    
- **`flatpak`** → قابل نصب روی Ubuntu است، ولی به‌صورت پیش‌فرض ابزار اصلی Ubuntu نیست.
    
- **`aptitude`** → جایگزین قدیمی‌تر برای `apt`، با قابلیت‌های تعاملی بیشتر.
    

پس اگر داری **Ubuntu Server** یاد می‌گیری، فعلاً تمرکزت را روی **`apt` + `dpkg`** بگذار؛ همین دو تا برای فهم سیستم مدیریت پکیج Ubuntu بسیار مهم‌اند.

[[apt چیست؟]]